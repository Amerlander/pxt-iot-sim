{
  "name": "pxt-iot-sim",
  "version": "0.1.0",
  "private": true,
  "description": "Simulator-Extension (simx) fuer MakeCode: zeigt die Feeds des verknuepften IoT-Dashboards neben dem Simulator.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3000"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
